# 줄바꿈은 저장소 안에서 LF 로 통일한다. 체크아웃 시 OS 에 맞춰 복원된다.
* text=auto

# 아래는 절대 텍스트로 다루지 않는다 (변환하면 파일이 깨진다)
*.slx   binary
*.slxc  binary
*.mat   binary
*.pdf   binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.x3d   binary
*.xlsx  binary
*.docx  binary
*.pptx  binary
*.hwp   binary
