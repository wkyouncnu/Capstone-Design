# 캡스톤디자인 — 폴더 안내

2026-09-05 정리. **파일은 하나도 삭제하지 않았고, 이동·이름 변경만 했다.**
빈 폴더 236개만 지웠다.

---

## 어디에 무엇이 있나

| 폴더 | 내용 |
|---|---|
| **`1_2026-2학기_강의자료/`** | **올해 강의. 이것만 보면 된다.** Obsidian 볼트 · 주차별 MD+PDF · Simulink 모델 |
| `2_지난학기_강의자료/` | `2025/` · `2026-1/` — 지난 학기 슬라이드, 특강, 논문, 동영상 |
| `3_예제코드/` | `CSEI/` · `PID_Practice/` — MATLAB·Simulink 실습 원본 |
| `4_참고자료/` | 캡스톤 운영·평가 서식, 논문작성법, 우수 사례 |
| `5_학생제출물/` | 조별 최종결과 · 개인 제출물 |
| `강의계획서_2026.pdf`<br>`출석부_2026.xlsx` | 학교 제출본 · 출결 기록 |

---

## 파일 이름 규칙

- 앞의 **두 자리 숫자가 분류이자 순서**다. 탐색기에서 그대로 정렬된다

| 번호대 | 2025 | 2026-1 | 참고자료 |
|---|---|---|---|
| 10~ | 강의 슬라이드 | 과목 소개·가이드 | 운영·소개 |
| 20~ | Simulink 교재 | 사례집·우수작품 | 평가 서식 |
| 30~ | 외부 특강 | 사업 공고 | 논문 작성법 |
| 40~ | 시험 문제 | 산업계 자료 | 우수 사례 |
| 50~ | — | 논문 | 회의록 |
| 60~ | — | 참조논문 모음 | — |
| 70~ | — | RISE 사업단 | — |
| 80~ | — | 동영상 | — |
| 90~ | 백업 | — | — |

- 학생 제출물은 **`학번_이름_과제`** 로 통일 (`202301714_신수민_중간고사`)
- 압축을 푼 폴더와 원본 `.zip` 을 **둘 다** 남겨 두었다

---

## 이름을 바꾸지 않은 것

| 대상 | 이유 |
|---|---|
| `1_2026-2학기_강의자료/` 안의 모든 파일 | 이미 `W01_…` 형식이고, 문서끼리 링크로 엮여 있다 |
| `3_예제코드/` 의 `.m` · `.slx` | 스크립트가 모델을 **이름으로** 부른다. 바꾸면 실행이 깨진다 |
| `2025/실습파일_MATLAB/` 의 18개 파일 | 위와 같은 이유. 문서와 섞이지 않게 폴더만 분리했다 |

---

## MATLAB 빌드 캐시

- `slprj/` 폴더 13개(하위 1,333파일)와 짝이 있는 `.slxc` 45개를 삭제했다 — **23.5 MB**
- 폴더 수 640 → **289**, 파일 수 2,776 → **1,398**
- 이것들은 **모델을 돌리면 MATLAB이 다시 만든다.** 삭제해도 잃는 것이 없다
  - 확인: 캐시를 지운 뒤 `W06_5_offline` 을 실행해 문서값과 같은 **1.333 m/s** 를 얻었고, 캐시는 자동 재생성되었다
- 작업하다 보면 다시 생긴다. 거슬리면 언제든 `slprj` 폴더를 통째로 지우면 된다

### 지우지 않은 백업 21개

- `.asv` `.autosave` `.original` `.r2022a` `.r2023b` — 저장하지 않은 편집분이 들어 있을 수 있다
- 그중 둘은 **원본이 없는 유일본**이므로 특히 지우면 안 된다

| 파일 | 사라진 원본 |
|---|---|
| `3_예제코드/CSEI/Control_Psi_Y_U_sim_CSEI_Scripts.asv` | `.m` 없음 |
| `3_예제코드/CSEI/Control_Psi_Y_U_sim_CSEI.slx.r2022a` | `.slx` 없음 |

- 짝을 잃은 `.slxc` 10개도 남겨 두었다

---

## GitHub 백업

- 원격 — `git@github.com:wkyouncnu/Capstone-Design.git` (브랜치 `main`)
- 올라가는 것은 **직접 만든 자료만** — 강의자료 MD·PDF·SVG, Simulink 모델, MATLAB 코드
- 올라가지 않는 것 — 학생 제출물·출결(개인정보), 교재·논문(저작권), 100 MB 초과 파일
- 세션이 끝날 때 자동으로 커밋·푸시된다. 직접 하려면

```bash
bash 1_2026-2학기_강의자료/_tools/git_autopush.sh
```

> [!important] 첫 푸시 전에 SSH 공개키를 GitHub 에 등록해야 한다
> `cat ~/.ssh/id_ed25519.pub` 의 출력을 https://github.com/settings/keys 에 등록.
> 자세한 절차와 오류 대처는 스킬 `capstone-git-sync` 참고.

---

## 정리 전에는 이랬다

```
캡스톤디자인/
├── 강의자료/                     ← 한 겹 더 있었다
│   ├── [2025] 강의자료/           45개 파일이 한 폴더에
│   ├── [2026-1] 강의자료/
│   └── [2026-2] 강의자료/         ← 올해 자료가 여기 묻혀 있었다
├── 예제코드/  참고자료/
└── 채첨/                         ← 오타
```

- 대괄호 `[ ]` 는 PowerShell·bash 에서 와일드카드로 해석되어 스크립트를 자주 깨뜨렸다.
  전부 없앴다
