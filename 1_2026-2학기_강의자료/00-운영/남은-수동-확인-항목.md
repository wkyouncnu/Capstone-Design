---
type: checklist
title: 남은 수동 확인 항목
date: 2026-09-21
tags: [course, upkeep, capture]
status: open
summary: 에이전트가 대신할 수 없는 항목 모음 — 화면 캡처 7건, 실행 출력 5건, 판단 4건. 처리하면 본문과 PDF 에 반영하고 이 표에서 지움
---

# 남은 수동 확인 항목

> [!important] 이 문서에 남는 것
> **사람이 직접 화면을 보거나 눌러야 하는 것**만 남김. 스크립트로 재현되는 수치·도면·문체는
> 이 문서가 아니라 `vault_check.sh` 와 각 주차 빌더가 맡음

- 기준일: 2026-09-21
- 처리 순서: 캡처·출력 확보 → 해당 MD 반영 → `bash _tools/pdf_sync.sh` → 이 문서의 해당 줄 삭제
- 그림은 `assets/` 에, 주차별 모델 도면은 `W0X_simulink/img/` 에 둠

---

## 1. 화면 캡처

| # | 주차 | 찍을 것 | 지금 상태 |
|---|---|---|---|
| 1 | 3 | RViz2 — Fixed Frame `wamv/wamv/base_link`, TF + PointCloud2 표시 | 3주차에 RViz 그림 없음. 본문 서술("배를 돌리면 TF 축은 제자리, 점군이 반대로 돎")이 실제와 맞는지 확인도 겸함 |
| 2 | 5 | WSL 에 연결된 VS Code 창 — 왼쪽 아래 `WSL: Ubuntu-22.04`, 오른쪽 Claude Code 패널 | `assets/w05-vscode-vault.png` 은 Windows 쪽 창이라 B-3 에서 참조를 뺀 상태 |
| 3 | 5 | Claude Code **실행** 화면 — C-5 첫 대화 또는 E-2 프롬프트 입력 | 공식 문서 캡처(`w05-claudecode-docs.png`)뿐. `week-quality-bar.md` §3 이 실행 화면을 요구 |
| 4 | 2 | turtlesim 첫 화면·spawn 화면 | `w02-turtlesim-start.png`, `w02-turtlesim-spawn.png` 이 확대된 창이라 거북이가 가운데가 아님 → 기본 창 크기로 |
| 5 | 6 | Simulink 편집기 창 | `w06-simulink-window.png` 의 상태 표시줄이 잘려 솔버 `ode4` 와 배율이 안 보임 |
| 6 | 6_0 | R2024b Simulink 편집기 전체 창 | 1-4 절(화면 구성)에 실제 창 캡처 없음 |
| 7 | 4 | 정지유지 과제 화면 | `w04-task-stationkeeping.png` 의 RTF 가 `−87.01 %` 로 잘못 찍힘 (본문에 표시 오류라는 설명은 붙여 둠) |

## 2. 실행해서 받을 출력

| # | 주차 | 받을 것 | 조건 |
|---|---|---|---|
| 8 | 5 | F-3 `/mcp` 확인 — Windows 쪽 확장 패널에서 실제로 동작하는지, `matlab` 항목이 어떻게 보이는지 | 확장에서 안 되면 PowerShell `claude mcp list` 처럼 **실제로 되는 명령**으로 본문 교체 |
| 9 | 5 | C-5 승인 요청 표의 원문 문구 — 폴더 신뢰 · 파일 수정 · 명령 실행 프롬프트 | 첫 실행 때만 뜸 |
| 10 | 5 | E-4 빌드 정상 출력 `colcon build --symlink-install --packages-select team_usv` | D-1 의 `ros2 pkg create` 와 entry_points 등록 뒤 |
| 11 | 2 | 2-9 QoS 진단 `ros2 topic info /qos_topic --verbose` 전체 출력 | 현재 본문은 볼 줄만 추린 발췌 |
| 12 | 2 | 2-4 액션 출력 — 목표 `{theta: -1.57}`, teleport(1.57) 직후 (delta ≈ 3.14 예상) | 현재 출력은 시작 선수각 약 −1.17 rad 조건. `Result: delta` 를 "시작 각 − 끝 각" 으로 설명해 둠 (turtlesim 소스 `rotate_absolute_start_orient_ - orient_` 로 한 번 확인 권장) |

## 3. 판단이 필요한 것

| # | 주차 | 무엇 | 선택지 |
|---|---|---|---|
| 13 | 5 | Claude Code 버전이 문서 안에서 둘로 갈림 — 확장 `v2.1.266`(C-1), CLI `2.1.237`(C-2, H) | 한 시점에 다시 캡처해 맞추거나, 버전 표기를 지우고 "설치 시점 최신" 으로 둠 |
| 14 | 2 | Domain ID 예시값 — 본문 셸·MATLAB 은 7, `W02_setup.m` 과 그 정상 출력은 8 | `W02_setup.m` 을 7 로 바꾸고 출력을 다시 받거나, 8 을 기준 환경 값으로 두는 현재 설명을 유지 |
| 15 | 2 | 과제 2 배점표 "두 노드 정상 동작 40 %" | `ros2 topic hz` 출력(50 Hz / 10 Hz) 첨부를 필수로 올릴지 — 배점 변경은 교수 결정 |
| 16 | 7 | 1-7 · 1-8 표의 평균 선회율 · 가로지름 · 전환 횟수 · 모서리 컷 · 8방향 완주 시각 | `W05_sweep.m` 으로 재현되므로 그대로 두거나, 다시 돌려 값을 갱신 |

---

## 최근 정리된 항목 (참고)

- 9주차 `W09_2_qos_test` 도면의 `rel_new` · `be_new` 라벨 겹침 — 2026-09-20 블록 겹침 정리로 해소
- 5주차 E-5 VRX 실행 출력 — 2026-09-19 재측정 때 실제 출력으로 교체
- 전 모델 배선 — 2026-09-21 기준 55개 모델이 일곱 지표(선 겹침 · 블록 관통 · 꺾임 3회+ · 매달림 · 사선 · 블록 겹침 · 이름표 위 선) 0

## 관련 문서

- [[강의계획서]] — 개강 전 준비 체크리스트
- `.claude/skills/capstone-lecture-vault/references/week-quality-bar.md` — 주차 완성도 합격선
- `.claude/skills/simulink-gnc-models/references/line-routing.md` — 도면 배선 합격선
