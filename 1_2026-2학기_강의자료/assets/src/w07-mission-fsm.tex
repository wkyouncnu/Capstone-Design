% assets/w07-mission-fsm.svg — 미션 상태기계
%
%   bash _tools/tikz2svg.sh assets/src/w07-mission-fsm.tex assets/w07-mission-fsm.svg
%
% 요점: 상태 3개와 전이 4개(기본 전이 포함). 7주차 유도와 8주차 로이터링을 하나의 임무로 잇는다.
%       아래 절반은 모델 구조 — 도착 신호가 차트로 들어가고, mode 는 한 스텝 늦춰(1/z) 유도로 돌아간다.
%
% 고친 것 (2026-09-18) — 기본 전이가 없었고, WpManager -> MissionFSM (at_loiter, at_end) 과
% mode 되먹임의 1/z 가 빠져 1-5절의 루프가 그림에 보이지 않았다. 주석 ① ② 도 1-4절과 맞춤.

\documentclass[border=6pt]{standalone}
\input{capstone-style}

\begin{document}
\begin{tikzpicture}[x=1mm, y=1mm]

% ---- 상태 -----------------------------------------------------------------
\node[gblk, minimum width=30mm, minimum height=12mm] (wp)  at (0,0)   {WpFollow};
\node[blk,  minimum width=30mm, minimum height=12mm, draw=pathc] (lo) at (52,0) {Loiter};
\node[blk,  minimum width=30mm, minimum height=12mm] (fi)  at (104,0) {Finish};

\node[note, below=0.8mm of wp] {\texttt{mode = 1} · LOS 유도};
\node[note, below=0.8mm of lo] {\texttt{mode = 2} · 벡터필드};
\node[note, below=0.8mm of fi] {\texttt{mode = 3} · 정지};

% 기본 전이 — 시작하면 WpFollow
\fill[ink] (-26,0) circle (1.2);
\draw[sig] (-24.8,0) -- (wp.west);
\node[note, anchor=south] at (-26,1.6) {시작};

% ---- 전이 -----------------------------------------------------------------
\draw[sig, accent] (wp.east) -- (lo.west);
\node[note, accent, above] at (26,3) {\texttt{[at\_loiter > 0.5]}};
\node[note, accent, anchor=north] at (19,-0.6) {순서 1};

\draw[sig, accent] (lo.south) .. controls +(-12mm,-17mm) and +(12mm,-17mm) .. (wp.south);
\node[note, accent] at (26,-21) {\texttt{[turns >= req\_turns]}};

\draw[sig, accent] (wp.north) .. controls +(0,12mm) and +(0,12mm) .. (fi.north);
\node[note, accent] at (52,14) {\texttt{[at\_end > 0.5]}};
\node[note, accent, anchor=east] at (-1,8.5) {순서 2};

% ---- 구조 -----------------------------------------------------------------
\node[gblk, minimum width=30mm, minimum height=9mm] (wm) at (0,-38)  {WpManager};
\node[blk, draw=pathc, minimum width=30mm] (lv) at (0,-50) {LoiterVF};
\node[blk, minimum width=30mm] (tc) at (0,-62)  {TurnCount};

\node[blk, draw=accent, minimum width=30mm, minimum height=11mm] (fsm) at (58,-58) {MissionFSM};
\node[note, below=0.6mm of fsm] {Stateflow};
\node[tblk, minimum width=26mm, minimum height=11mm] (ms) at (108,-38) {ModeSwitch};
\node[blk, minimum width=10mm, minimum height=7mm] (dly) at (100,-72) {$1/z$};
\node[note, anchor=west] at (106,-72) {ModeDly};

% 두 유도 출력 -> ModeSwitch (둘 다 늘 계산한다)
\draw[wire] (15,-35) -- (95,-35);
\node[sname, font=\scriptsize, anchor=south] at (58,-35) {\texttt{psi\_los}};
\draw[wire] (lv.east) -- (84,-50) |- (95,-38);
\node[sname, font=\scriptsize, anchor=south] at (58,-50) {\texttt{psi\_lo}};

% 도착 신호 · 회전수 -> 차트
\draw[wire] (15,-40.5) -- (26,-40.5) |- ($(fsm.west)+(0,2.5)$);
\node[sname, font=\scriptsize, anchor=north west] at (16,-40.8) {\texttt{at\_loiter, at\_end}};
\draw[wire] (tc.east) -- (26,-62) |- ($(fsm.west)+(0,-2.5)$);
\node[sname, font=\scriptsize, anchor=north west] at (16,-62.3) {\texttt{turns}};

% mode -> ModeSwitch (바로), -> 1/z -> 유도 (한 스텝 뒤)
\draw[sig, accent] (fsm.east) -- (90,-58) |- (95,-41);
\draw[sig, accent] (90,-58) |- (dly.west);
\node[note, accent, anchor=south] at (81,-58) {\texttt{mode}};
\draw[sig, accent] (dly.south) -- (100,-82) -| (-20,-38) -- (wm.west);
\draw[sig, accent] (-20,-62) -- (tc.west);
\node[note, accent, anchor=north] at (40,-82.6) {\texttt{mode} (한 스텝 전) — 대수 루프를 끊는 자리};
\draw[sig] (ms.east) -- ++(8,0) node[note, anchor=west] {내부 루프};

% ---- 유의 사항 -------------------------------------------------------------
\node[wnote, anchor=west, text width=150mm] at (-26,-98)
     {\textbf{만들면서 실제로 밟은 문제}\\[2pt]
      \textcircled{1} 로이터 원이 다음 웨이포인트를 지나가면 인덱스가 올라가 그 점을 건너뜀 → 인덱스를 \texttt{mode} 로 잠금 (1-4절)\\
      \textcircled{2} 로이터 중심을 "현재 웨이포인트" 로 잡으면 인덱스가 움직일 때 원이 통째로 이동 → 중심을 상수로 고정\\
      \textcircled{3} \texttt{mode} 되먹임으로 대수 루프 → $1/z$ 로 한 스텝 늦춤 (1-5절)};

\end{tikzpicture}
\end{document}
