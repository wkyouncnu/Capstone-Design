% assets/w10-vscode-claude.svg — 무엇이 어디에 설치되는가
%
%   bash _tools/tikz2svg.sh assets/src/w10-vscode-claude.tex assets/w10-vscode-claude.svg
%
% 요점: ROS 2 코드를 다루는 Claude Code 는 WSL 쪽에, MATLAB 을 부르는 Claude 는 Windows 쪽에 둔다.
%       MATLAB MCP 서버는 Windows 의 MATLAB 에 붙는 Windows 프로그램이다.
%
% 고친 것 (2026-09-18) — MCP 서버를 WSL 상자에 그려 두었는데, 5주차 F-3 의 설정은 Windows 쪽
% (C:\Users\<사용자>\.claude.json) 에 들어간다. WSL 의 claude 는 그 설정을 읽지 않는다.

\documentclass[border=6pt]{standalone}
\input{capstone-style}

\begin{document}
\begin{tikzpicture}[x=1mm, y=1mm]

% ===================== Windows =====================
\node[draw=soft, line width=0.6pt, rounded corners=1.5mm,
      minimum width=72mm, minimum height=84mm] (win) at (-40,-8) {};
\node[font=\small\bfseries, anchor=north west] at ($(win.north west)+(3mm,-3mm)$)
     {Windows 11 쪽};

\node[nblk, minimum width=52mm, minimum height=11mm] (vs) at (-40,20) {VS Code};
\node[note, below=0.6mm of vs] {편집기 본체 — Windows 에 설치};

\node[nblk, minimum width=52mm, minimum height=11mm] (ext) at (-40,3) {WSL 확장 (Remote -- WSL)};
\node[note, below=0.6mm of ext] {두 세계를 잇는 다리};

\node[cblk, minimum width=52mm, minimum height=11mm] (ml) at (-40,-14) {MATLAB R2024b};
\node[note, below=0.6mm of ml] {Simulink · ROS Toolbox};

\node[tblk, minimum width=52mm, minimum height=11mm] (mcp) at (-40,-33) {Claude Code 확장 + MATLAB MCP};
\node[note, below=0.6mm of mcp] {MATLAB·Simulink 작업은 여기서 (F-3)};
\draw[fb] (mcp.north) -- (ml.south);

% ===================== WSL =====================
\node[draw=plant, line width=0.7pt, rounded corners=1.5mm,
      minimum width=72mm, minimum height=84mm] (wsl) at (46,-8) {};
\node[font=\small\bfseries, plant, anchor=north west] at ($(wsl.north west)+(3mm,-3mm)$)
     {WSL2 — Ubuntu 22.04 쪽};

\node[blk, draw=accent, minimum width=52mm, minimum height=11mm] (cc) at (46,20) {Claude Code (CLI)};
\node[note, accent, below=0.6mm of cc] {\textbf{ROS 2 코드용은 여기에 설치한다}};

\node[pblk, minimum width=52mm, minimum height=11mm] (ros) at (46,3) {ROS 2 · VRX · 작성한 코드};
\node[note, below=0.6mm of ros] {1$\sim$4주차에 만든 것};

% ===================== 연결 =====================
\draw[sig] (ext.east) -- node[sname, above, font=\scriptsize] {접속} (ext -| wsl.west);
\draw[fb]  (cc.south) -- (ros.north);

% ===================== 경고 =====================
\node[wnote, anchor=north, text width=150mm, align=center] at (3,-54)
     {\textbf{ROS 2 코드를 다루는 Claude Code CLI 는 WSL 에 설치한다.}
      Windows 쪽에 설치하면 WSL 안의 ROS 2 코드와 빌드 결과를 보지 못한다.\\
      반대로 MATLAB MCP 설정은 Windows 쪽 파일에 들어가므로, MATLAB 을 부르는 일은 Windows 의 VS Code 확장이 맡는다};

\end{tikzpicture}
\end{document}
