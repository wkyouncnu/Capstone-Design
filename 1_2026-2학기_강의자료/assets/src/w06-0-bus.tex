% assets/w06-0-bus.svg — 버스
%
%   bash _tools/tikz2svg.sh assets/src/w06-0-bus.tex assets/w06-0-bus.svg
%
% 요점: 묶을 때 이름이 붙고, 풀 때 그 이름으로 꺼낸다.
%       중첩된 버스는 꺼낼 때 전체 경로를 적어야 한다.
%
% 고친 것 (2026-09-19) — Bus Creator 를 주황 상자로, Inner/Outer 를 따로 보라 상자로 그렸었다.
% Simulink 에서 Bus Creator 는 검은 세로 막대이고 Inner/Outer 는 그 막대(블록)의 이름이자 나가는 선의 이름이다.

\documentclass[border=6pt]{standalone}
\input{capstone-style}

\begin{document}
\begin{tikzpicture}[x=1mm, y=1mm]

% ===================== 묶기 =====================
\node[font=\small\bfseries, anchor=west] at (-6,30) {① 묶는다 — Bus Creator};

\node[blk, minimum width=13mm, minimum height=7mm] (s1) at (0,20)  {1};
\node[blk, minimum width=13mm, minimum height=7mm] (s2) at (0,11)  {2};
\node[blk, minimum width=13mm, minimum height=7mm] (s3) at (0,2)   {3};
\node[note, left=1mm of s1] {$x$};
\node[note, left=1mm of s2] {$y$};
\node[note, left=1mm of s3] {$z$};

\node[draw=ink, fill=ink, minimum width=2.2mm, minimum height=26mm, inner sep=0pt] (bc) at (26,11) {};
\node[note, anchor=north, align=center] at (26,-2.8) {\texttt{Inner}\\(Bus Creator)};

\draw[sig] (s1.east) -- (s1.east -| bc.west);
\draw[sig] (s2.east) -- (s2.east -| bc.west);
\draw[sig] (s3.east) -- (s3.east -| bc.west);

\coordinate (inner) at (58,11);
\draw[wire, line width=1.1pt] (bc.east) -- (inner);
\node[font=\scriptsize\ttfamily, above] at (44,11.3) {Inner};
\node[note, below] at (44,10.2) {세 개가 한 줄로};

\node[note, anchor=west, text width=44mm] at (-6,-17)
     {\textbf{선 이름이 곧 원소 이름이 된다.}
      이름 없는 선을 묶으면 \texttt{signal1} 같은 이름이 자동으로 붙어
      나중에 찾기 어렵다};

% ===================== 중첩 =====================
\node[font=\small\bfseries, anchor=west] at (78,30) {② 다시 묶는다};

\node[blk, minimum width=16mm, minimum height=7mm] (st) at (86,2) {12:34};
\node[note, left=1mm of st] {\texttt{stamp}};

\node[draw=ink, fill=ink, minimum width=2.2mm, minimum height=22mm, inner sep=0pt] (bc2) at (110,11) {};
\node[note, anchor=north, align=center] at (110,-1.2) {\texttt{Outer}\\(Bus Creator)};
\draw[sig, line width=1.1pt] (inner) -- ++(4mm,0) |- (104,18) -- (104,18 -| bc2.west);
\draw[sig] (st.east) -- (st.east -| bc2.west);

\coordinate (outer) at (140,11);
\draw[sig, line width=1.3pt] (bc2.east) -- (outer);
\node[font=\scriptsize\ttfamily, above] at (126,11.3) {Outer};

\node[note, anchor=north west, align=left] at (78,-6)
     {\texttt{Outer} 의 구조\\
      \quad \texttt{Inner.x = 1}\\
      \quad \texttt{Inner.y = 2}\\
      \quad \texttt{Inner.z = 3}\\
      \quad \texttt{stamp = 12:34}};

% ===================== 풀기 =====================
\node[font=\small\bfseries, anchor=west] at (-6,-34) {③ 꺼낸다 — Bus Selector};

\coordinate (o2) at (8,-46);
\node[font=\scriptsize\ttfamily, above] at (22,-45.7) {Outer};
\node[draw=ink, fill=ink, minimum width=2.2mm, minimum height=18mm, inner sep=0pt] (bs) at (46,-46) {};
\node[note, anchor=north] at (46,-55.5) {Bus Selector};
\node[blk, minimum width=13mm, minimum height=7mm] (out) at (76,-46) {2};

\draw[sig, line width=1.3pt] (o2) -- (bs.west);
\draw[sig] (bs.east) -- (out.west);
\node[note, above] at (61,-44) {\texttt{Inner.y}};

\node[wnote, anchor=west, text width=76mm] at (94,-46)
     {꺼낼 때는 \textbf{전체 경로}를 적는다. \texttt{y} 만 쓰면 찾지 못한다.
      중첩이 깊어질수록 이 규칙이 중요해진다};

\end{tikzpicture}
\end{document}
