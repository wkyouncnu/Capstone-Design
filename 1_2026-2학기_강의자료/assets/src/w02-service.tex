% assets/w02-service.svg — 서비스: 요청과 응답, 그리고 서버는 하나
%
%   bash _tools/tikz2svg.sh assets/src/w02-service.tex assets/w02-service.svg
%
% 요점: 토픽은 흘려보내고 잊는다. 서비스는 부르고 답을 기다린다. 서버는 딱 하나다.

\documentclass[border=6pt]{standalone}
\input{capstone-style}

\begin{document}
\begin{tikzpicture}[x=1mm, y=1mm]

% ===================== 노드를 먼저 다 놓는다 =====================
\node[nblk, minimum width=36mm, minimum height=26mm] (c1) at (0,10)
     {서비스 \textbf{클라이언트} A};

\node[pblk, minimum width=36mm, minimum height=26mm] (srv) at (104,10)
     {서비스 \textbf{서버}};
\node[note, below=1.2mm of srv, text width=40mm, align=center]
     {\texttt{/spawn} 을 제공하는 노드\\ \textbf{서버는 하나뿐}};

\node[nblk, minimum width=36mm, minimum height=11mm] (c2) at (0,-16)
     {서비스 \textbf{클라이언트} B};
\node[note, below=1.0mm of c2, text width=40mm, align=center]
     {클라이언트는 여럿이어도 된다};

\node[blk, draw=accent, minimum width=28mm, minimum height=8mm] (req) at (52,19)
     {\textbf{Request} 요청};
\node[blk, draw=warn, minimum width=28mm, minimum height=8mm] (res) at (52,1)
     {\textbf{Response} 응답};

% ===================== 요청 차선 (위) =====================
\draw[sig, accent] ($(c1.east)+(0,7)$) -- (req.west);
\draw[sig, accent] (req.east) -- (req -| srv.west);
\node[note, accent, anchor=south] at (52,24) {``거북이 한 마리 더''};

% ===================== 응답 차선 (아래) =====================
\draw[sig, warn] (res -| srv.west) -- (res.east);
\draw[sig, warn] (res.west) -- ($(c1.east)+(0,-7)$);
\node[note, warn, anchor=north] at (52,-4) {``만들었다. 이름은 turtle2''};

% ===================== 클라이언트 B 도 같은 서비스를 쓴다 =====================
\draw[sig, accent, densely dashed] (c2.east) -| (req.south);

% ===================== 토픽과의 차이 =====================
\node[wnote, anchor=north, text width=142mm, align=center] at (52,-30)
     {\textbf{토픽과 다른 점} — 클라이언트는 답이 올 때까지 \textbf{기다린다}.
      서버가 없으면 그 자리에서 멈춘 채 대기한다.
      \texttt{ros2 service call} 이 응답 없이 멈춰 있으면 \textbf{서버 노드가 안 떠 있는 것}이다};

\end{tikzpicture}
\end{document}
